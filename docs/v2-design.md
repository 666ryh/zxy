# 紫色个人手账 v2

用户确认：仅网页预览，待全部确认后再打包。紫色库洛米风格、我的页面、底薪2000、90分钟课时费（不足算90分钟）、自招学生招生提成、每次固定晚辅费。邮箱验证码替代短信。人工客服辉辉由用户本人在微信接待。

视觉：淡紫#F7F3FF，主紫#7952B3，深紫#382545，粉#EFADD2，白色纸面。以库洛米矢量头像、蝴蝶结、星星点缀个人手账，不降低文字可读性。底部五项：课表、学生、薪资、考勤、我的。

薪资：月度独立设置底薪2000与晚辅每次单价×次数。新课程每90分钟单价，按ceil(分钟/90)计课时（明确展示），每节快照。旧版课程与学生保留按小时标识和金额，编辑时可选择换用新标准。招生提成逐笔记录学生、缴费日期、实缴金额、比例，按缴费月份计入一次；不同缴费金额不直接乘统一人数。归档不删除财务记录。

登录：Node本地服务，邮箱OTP、5分钟过期、60秒重发、5次错误、每IP限流；随机会话HttpOnly/SameSite cookie。登录和登出清理当前界面状态，每邮箱独立本机数据。原访客数据不自动转移到任何邮箱，提供明确导入确认。开发邮件不发送，验证码仅在本机模式返回；SMTP配置时由nodemailer发送，不在前端暴露凭据。当前仅本地预览，公网前需HTTPS、SMTP、持久账号/会话等上线配置。

个人中心：头像、昵称、邮箱、个人简介、月度统计、编辑资料、薪资设置、辉辉微信客服、数据备份、关于与退出登录。微信注册手机号15048452629，提示搜索结果受微信隐私设置影响，不伪造聊天和在线状态。

鸿蒙：用户截图确认华为nova 13（BLK-AL80）、HarmonyOS 6.1.0、API 6.1.1（24）、OpenHarmony 6.1。后续以该机为适配与真机验收目标，计划采用ArkUI Web容器、原生存储和签名分发路线；现有APK不作为该机交付方案。仍先完成浏览器页面，全部确认后再开发原生适配和打包，当前不生成任何包。

实现：先薪资迁移和边界测试→后台OTP测试与实现→个人中心和主题/薪资表单→浏览器验收。测试包括历史计费保留、90分钟取整、提成防重复、月度隔离、邮箱认证错误/过期/重放/登出。
