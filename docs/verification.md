# 验证记录 · 2026-09-24

- Node 18.20.7：11 项行为测试通过（9 项核心功能 + 2 项检查后新增的备份回归测试）。
- 先验证缺少 domain 模块时测试失败，再实现；新增两个备份测试均复现失败后修复至通过。
- Chrome 手机视口：新建“测试同学”，100.10元时薪；排9月23日09:00–10:30课程；补录完成；标记已收款。界面显示课酬150.15元、1.5小时、1天出勤、待收0元。
- 重新加载保持记录；JSON 备份恢复确认后仍为1节已完成、已收款。
- 375×812 竖屏、812×375 横屏页面宽度无横向溢出；390×844 检查课表、薪资截图。
- axe-core 4.12.1：课表与薪资页 WCAG2A/2AA 自动检查0项违规。仅覆盖自动检测项，不代表完整无障碍认证。
- SDK 下载 SHA1 与 Google 仓库元数据一致。APK 编译成功，apksigner v2/v3 验证通过；aapt 识别应用名“课笺”、包名cn.kejian.teacher、最低API26、目标API34。
- 检查发现导入编号可注入HTML、中文UTF8字节与字符上限不一致，两项均添加回归测试并修复。保存前统一经过备份校验，Android写入前再次验证字节上限与记录数。

## 验证限制

当前 adb 无连接设备，未运行安卓真机/模拟器。WebView原生桥、系统文件选择器、安卓返回键和系统字体缩放已编译但尚未进行设备交互验证。系统WebView版本过旧可能不支持界面使用的现代浏览器特性；建议安装后先使用示例数据验收。
