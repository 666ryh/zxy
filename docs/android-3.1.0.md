# 课笺 3.1.0 云端版 APK

安装包：`releases/kejian-3.1.0.apk`，193567 字节。包名 `cn.kejian.teacher`，versionCode 310，最低 Android 8（API 26），目标 API 34。

启动访问 `https://www.ryh6666.xyz/`，使用正式站的邮箱注册/登录和全量账号同步。首次打开需要网络；普通离线修改在已加载页面中先保存，恢复网络后重试上传。不会在网络错误时把旧访客数据直接并入邮箱账号。

与 3.0.4 使用相同签名，可尝试直接覆盖安装，请勿先卸载清除旧数据。在主页按系统返回键，退出提示中选择“旧版本机数据”，可打开保留的本地存储并导出备份；再按返回键选择“返回云端”，登录后导入。网络加载错误页也提供旧数据入口。

只允许正式站 HTTPS 和受控本机资源入口，禁止明文访问和其他站点导航，保留系统相册选择和备份导出。云端内容更新随正式站发布，无需重新打 APK。

验证：63 项 JavaScript 测试通过；Java URL 白名单测试通过；APK 内置 7 项资源检查通过；apksigner v2/v3 签名验证通过，新旧包证书一致；aapt 确认版本号、网络权限和最低版本。当前 adb 无设备，未验证真机安装、HarmonyOS WebView、相册和升级迁移操作。

SHA-256：`AE3D23662243AD6DFD78B1FD41A8E1CA82B5153A2088B1F29E0319B9541C1E6D`
